import 'package:flutter/material.dart';

// Completely different colors for a unique look
const Color greyColor = Color(0xFF9B59B6); // Purple for a modern touch
const Color redColor = Color(0xFFFF6347); // Tomato Red for vibrancy
const Color yellowColor = Color(0xFF00FF00); // Neon Green for a bold contrast

const headingTextStyle = TextStyle(color: Colors.white); // No changes to text style
